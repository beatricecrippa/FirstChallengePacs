#ifndef DATA_H
#define DATA_H

#include <stdio.h>

// Define the struct

struct Data{
    // function wrapper f
    // function wrapper nabla f
    // initial conditions
    // tolerances
    // initial step
    // maximal number of iterations
    int member1;
};

#endif // DATA_H
